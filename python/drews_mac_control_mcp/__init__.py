"""PyPI wrapper for Drew's Mac Control MCP Server.

The package exists to deliver and keep current the signed macOS app that does the real work; the
MCP server itself is the `MacControlRelay` binary inside that bundle. See `bootstrap`.
"""
